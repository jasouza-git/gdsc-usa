export type Member = {
    name: string,
    position: string,
    avatar?: string | null,
    color?: string | null,
    summary?: string | null,
    fbLink?: string | null,
    lnLink?: string | null,
    itLink?: string | null
}